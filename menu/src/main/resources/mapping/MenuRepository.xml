<?xml version='1.0'?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiaheliu.repository.MenuRepository">
    <select id="findAll" resultType="Menu">
        select m.id,m.name,m.price,m.size from t_menu m limit #{param1},#{param2}
    </select>

    <select id="count" resultType="int">
        select count(*) from t_menu
    </select>

    <select id="findById" resultType="Menu">
        select id mid,name mname,price,size from t_menu where id = #{id}
    </select>

    <insert id="save" parameterType="Menu">
        insert into t_menu(name, price, size) values((#{name},#{price},#{size})
    </insert>

    <update id="update" parameterType="Menu">
        update t_menu set name = #{name},price = #{price},size = #{size} where id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        delete from t_menu where id = #{id}
    </delete>
</mapper>